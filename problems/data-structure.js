'use strict';

/**
 * Take a look at the data structure in `problems/data/people.js`
 *
 * This data structure is indicitive of a different type of data structure, but
 * it is currently not represented in the current data structure. What would you
 * change about this data structure to make it more representative of what it's
 * meaning to be?
 *
 * Then, write a function that puts this array into that data structure.
 */

function changeStructure(people) {

}

module.exports = changeStructure;
