'use strict';

/**
 * Here you must export a function that takes in a string, reverses the words,
 * and returns the string with the words reversed.
 *
 * @param {String} str - The string whose words are to be reversed
 * @returns {String} The reversed sentence
 *
 * @example
 * reverse('Hello world.') === 'world. Hello';
 * reverse('correct horse battery staple') === 'staple battery horse correct';
 */
module.exports = function reverse(str) {

};
