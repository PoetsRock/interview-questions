'use strict';

/**
 * Write a function that takes in a string and returns false if there are
 * duplicate characters, and true if there aren't. Words are groups of
 * characters separated by spaces.
 *
 * @param {String} str - The string to check
 * @returns {Boolean} Whether or not the str has all unique characters
 *
 * @example
 * unique('vivint') === false // two 'v's and two 'i's
 * unique('store') === true   // all unique
 * unique('test') === false   // two 't's
 */
module.exports = function unique(str) {

};
